[pytest]
# Configure pytest markers and timeout

markers =
    slow: marks tests as slow (deselect with '-m "not slow"')

# Default timeout for all tests (5 min)
timeout = 300

# For slow tests, timeout is set per-test with @pytest.mark.timeout(360)
